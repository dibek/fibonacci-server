apiVersion: apps/v1
kind: Deployment
metadata:
  name: fibonacci-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fibonacci-server
  template:
    metadata:
      labels:
        app: fibonacci-server
    spec:
      containers:
      - name: fibonacci-server
        image: <your-docker-image>
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /fibonacci
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /fibonacci
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 3
